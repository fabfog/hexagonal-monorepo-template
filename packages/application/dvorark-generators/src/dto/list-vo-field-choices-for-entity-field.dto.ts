export interface ListVoFieldChoicesForEntityFieldInputDto {
  workspaceRoot: string;
  /** Domain package folder name under `packages/domain` (e.g. `user`, `core`). */
  entityDomainPackage: string;
}
