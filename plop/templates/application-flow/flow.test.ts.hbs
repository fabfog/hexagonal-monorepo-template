import { describe, it } from "vitest";
import {
  {{pascalCase flowName}}Flow,
  type {{pascalCase flowName}}FlowDependencies,
} from "./{{kebabCase flowName}}.flow";

// NOTE: interaction port is injected from composition layer, so here we can pass a simple stub
const interactionPortStub = {};

const deps: {{pascalCase flowName}}FlowDependencies = {
  interactionPort: interactionPortStub,
};

/**
 * Deliberately failing scaffold: replace this file after implementing
 * `./{{kebabCase flowName}}.flow.ts` and the interaction port.
 */
describe("{{pascalCase flowName}}Flow", () => {
  it("fails until you implement real flow tests", () => {
    void new {{pascalCase flowName}}Flow(deps);
    throw new Error(
      "Generator scaffold: implement {{pascalCase flowName}}Flow and port stubs, then delete this test and add behaviour-driven cases.",
    );
  });
});
