import type { {{pascalCase flowName}}InteractionPort } from '../ports/{{kebabCase flowName}}-interaction.port';

export class {{pascalCase flowName}}Flow {
  constructor(
    private readonly interactionPort: {{pascalCase flowName}}InteractionPort
  ) {}

  async execute(): Promise<void> {
    // TODO: implement flow
  }
}

